import React, {useState} from "react";
import { Controls } from "../";

type PomodoroProps={}

export const Pomodoro = ({ }: PomodoroProps) => {
    return (
        <div>
            Pomodoro comes here.
        </div>
    )
}
    
